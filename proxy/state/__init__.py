"""Server-side working state, all disk-backed under `data/`. Distinct from
`proxy.config`, which is the server's own configuration: these are the mutable
stores the server reads and writes while running."""
