"""Text cleaning and formatting. No I/O, no card structure -- these modules take
strings and return strings, and every one of them is driven by a real-corpus
finding rather than a general rule (see each module's docstring)."""
